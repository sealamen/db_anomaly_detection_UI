import streamlit as st


def run():
    st.title("서비스 아키텍쳐")

    # 이미지 표시
    st.image("assets/학습아키텍처(대체예정).PNG", caption="Milestone Roadmap", use_container_width=True)

    # 설명 텍스트
    st.markdown("""
    ### 프로젝트 주요 단계
    - 어떤 구조로 구성되어있으며 어떤 식으로 동작하는지 (PPT) ( OCI 대신 RTS 로 구현했다고 쓰자)
    - 프로젝트 별 폴더 구조 (UI, AI , Backend)

    ---
    각 단계가 완료될 때마다 마일스톤을 갱신하며, 전체 일정과 성과를 추적할 수 있습니다.
    """)

    # 이미지 표시
    st.image("assets/탐지아키텍처(대체예정).PNG", caption="Milestone Roadmap", use_container_width=True)

    # 설명 텍스트
    st.markdown("""
    ### 프로젝트 주요 단계
    1. **요구사항 분석 및 기획 완료**
    2. **데이터 파이프라인 구축**
    3. **모델 학습 및 성능 검증**
    4. **서비스 배포 및 모니터링 체계 구축**

    ---
    각 단계가 완료될 때마다 마일스톤을 갱신하며, 전체 일정과 성과를 추적할 수 있습니다.
    """)




st.title("서비스 아키텍쳐")

# 이미지 표시
st.image("assets/학습아키텍처(대체예정).PNG", caption="Milestone Roadmap", use_container_width=True)

# 설명 텍스트
st.markdown("""
### 프로젝트 주요 단계
- 어떤 구조로 구성되어있으며 어떤 식으로 동작하는지 (PPT) ( OCI 대신 RTS 로 구현했다고 쓰자)
- 프로젝트 별 폴더 구조 (UI, AI , Backend)

---
각 단계가 완료될 때마다 마일스톤을 갱신하며, 전체 일정과 성과를 추적할 수 있습니다.
""")

# 이미지 표시
st.image("assets/탐지아키텍처(대체예정).PNG", caption="Milestone Roadmap", use_container_width=True)

# 설명 텍스트
st.markdown("""
### 프로젝트 주요 단계
1. **요구사항 분석 및 기획 완료**
2. **데이터 파이프라인 구축**
3. **모델 학습 및 성능 검증**
4. **서비스 배포 및 모니터링 체계 구축**

---
각 단계가 완료될 때마다 마일스톤을 갱신하며, 전체 일정과 성과를 추적할 수 있습니다.
""")
